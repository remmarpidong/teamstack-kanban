// For now, disable middleware auth protection
// Will enable once next-auth is properly configured

// export { default } from "next-auth/middleware"

// export const config = {
//   matcher: [
//     '/((?!api/auth|_next/static|_next/image|favicon.ico).*)',
//   ],
// }
